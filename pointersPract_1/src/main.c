#include <avr/io.h>
#include <util/delay.h>

#define MAX 5
#define SEVENFOLD

void printArray(int arr[]) {
}

void makeArray(int max) {

}

int main() {
  int array1[MAX];
  printArray(array1);
  return 0;
}